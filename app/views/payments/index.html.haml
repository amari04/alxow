%h1 Сведения об оплате

%table.table.table-striped.table-hover
  %thead
    %tr
      %th Студент
      %th Стоимость
      %th Статус оплаты
      %th
      %th
      %th

  %tbody
    - @payments.each do |payment|
      %tr
        %td= payment.student
        %td= payment.cost
        %td= payment.status
        %td= link_to 'Show', payment
        %td= link_to 'Edit', edit_payment_path(payment)
        %td= link_to 'Destroy', payment, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'Добавить', new_payment_path, class: "btn btn-success"

%table.table.table-striped.table-hover
  %thead
    %tr
      %th Сотрудник
      %th Стоимость
      %th Статус оплаты
      %th
      %th
      %th

  %tbody
    - @payments.each do |payment|
      %tr
        %td= payment.worker
        %td= payment.cost
        %td= payment.status
        %td= link_to 'Show', payment
        %td= link_to 'Edit', edit_payment_path(payment)
        %td= link_to 'Destroy', payment, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'Добавить', new_payment_path, class: "btn btn-success"

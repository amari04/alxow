%h1 Listing guests

%table
  %thead
    %tr
      %th Person
      %th Start date
      %th Finish date
      %th Event
      %th
      %th
      %th

  %tbody
    - @guests.each do |guest|
      %tr
        %td= guest.person
        %td= guest.start_date
        %td= guest.finish_date
        %td= guest.event
        %td= link_to 'Show', guest
        %td= link_to 'Edit', edit_guest_path(guest)
        %td= link_to 'Destroy', guest, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Guest', new_guest_path

%h1 Список людей

%table.table.table-striped.table-hover
  %thead
    %tr
      %th id
      %th Фамилия
      %th Имя
      %th Очество
      %th Дата рождения
      %th Пол
      %th Паспорт серия
      %th Номер
      %th Кем вывдан
      %th Когда выдан
      %th Где выдан
      %th
      %th
      %th

  %tbody
    - @people.each do |person|
      %tr
        %td= person.user.email
        %td= person.last_name
        %td= person.first_name
        %td= person.second_name
        %td= person.birthday
        %td= person.sex
        %td= person.series
        %td= person.number
        %td= person.organization
        %td= person.pass_date
        %td= person.address
        %td= link_to 'Показать', person, class: "btn btn-success"
        %td= link_to 'Редактировать', edit_person_path(person), class: "btn btn-info"
        %td= link_to 'Удалить', person, :method => :delete, :data => { :confirm => 'Удалить?' }, class: "btn btn-danger"

%br

= link_to 'Добавить', new_person_path, class: "btn btn-success"
